import unittest


class TestMain(unittest.TestCase)
    def intiate_order(
            symbol,
            price,
            qty=1,
            order_type='market',
            side=None,
            time_in_force='day',
            limit_price=None,
